defmodule ETH.StorageA do
  alias Enum

  @var_name :number

  @spec store(uint256(), uint256()) :: nil
  def store(num, fake_num) do
    test = 123
    BlockchainStorage.store(@var_name, 12332322)
    BlockchainStorage.store(@var_name, num)
  end

  @spec retrieve() :: u256()
  def retrieve() do
    BlockchainStorage.get(@var_name)
  end

  defp abc(qwe) do
    store(qwe)
    &retrieve/2
&(&1+ &2)
    a = fn a,b -> 
    a+b 
    a-b
    123
    a
    :a
    end
  end
end
